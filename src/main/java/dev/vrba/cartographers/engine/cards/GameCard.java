package dev.vrba.cartographers.engine.cards;

/**
 * A common supertype for all game cards, so they can be stored in a deck without funky types like Scala's Either[A,B,C]
 */
public interface GameCard {
}
